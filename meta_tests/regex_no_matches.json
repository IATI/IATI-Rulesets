{
    "//iati-activity": {
        "regex_no_matches": {
            "cases": [ {
                "paths": [ "title" ],
                "regex": "^[^\\/\\&\\|\\?]+$"
             } ]
        }
    }
}

